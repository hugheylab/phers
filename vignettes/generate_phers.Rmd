---
title: "generate_phers"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{generate_phers}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}
library('phers')
library('data.table')
library('foreach')
library('doParallel')
registerDoParallel(cores = 2)
```


```{r}
phecodes = mapICDToPhecode(icdSample)
weights = calcWeights(demoSample, phecodes)
# diseasePhecodeMap = mapDiseaseToPhecode(c(154700))

calcPheRS(demoSample, phecodes, weights, diseaseIDs = c(154700))
```

